apiVersion: 1
providers:
  - name: QA Pipeline Dashboards
    orgId: 1
    folder: QA Pipeline
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards
